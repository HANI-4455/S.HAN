# FOOD 제품 생산 Check-list — 설치 및 운영 안내

PC · 태블릿 브라우저에서 그대로 쓰는 웹 프로그램입니다. 설치 앱이 아니라 주소만 열면 됩니다.

| 파일 | 역할 |
|---|---|
| `food_checklist.html` | 화면 (점포 · 지역 관리자 · 전체 관리자 공용) |
| `food_checklist_gas.js` | 데이터 저장 서버 (Google Apps Script) |

---

## 1. 서버 만들기 (최초 1회, 전체 관리자만)

1. [구글 시트](https://sheets.new)를 새로 만들고 이름을 `FOOD 체크리스트 DB` 로 지정합니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script** 를 엽니다.
3. 편집기에 있던 내용을 모두 지우고 `food_checklist_gas.js` 전체를 붙여넣은 뒤 저장합니다.
4. 상단 함수 목록에서 **`setup`** 을 선택하고 **실행** 을 누릅니다.
   - 처음 실행하면 권한 승인 창이 뜹니다. `고급 → 안전하지 않음(이동)` 을 눌러 본인 계정으로 승인합니다.
   - 실행이 끝나면 시트에 `Regions` `Users` `Products` `Records` 탭과 기본 제품 16종이 생성됩니다.
5. **배포 → 새 배포 → 유형 선택(톱니바퀴) → 웹 앱** 을 고르고 아래처럼 설정합니다.

   | 항목 | 값 |
   |---|---|
   | 실행 사용자 | **나** |
   | 액세스 권한이 있는 사용자 | **모든 사용자** |

6. 배포 후 표시되는 **웹 앱 URL** (`https://script.google.com/macros/s/.../exec`) 을 복사해 둡니다.

> 코드를 수정한 뒤에는 반드시 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포** 를 해야 반영됩니다. 저장만으로는 반영되지 않습니다.

---

## 2. 접속하기

주소: **https://hani-4455.github.io/S.HAN/food_checklist.html**

처음 접속한 기기에서 한 번만 **서버 주소** 칸에 위에서 복사한 웹 앱 URL 을 넣고 로그인합니다. 이후에는 그 기기에 저장되어 다시 묻지 않습니다.

태블릿에서는 브라우저 메뉴의 **홈 화면에 추가** 를 하면 앱처럼 전체 화면으로 열립니다.

### 기본 계정 (setup 실행 시 자동 생성)

| 아이디 | 비밀번호 | 권한 |
|---|---|---|
| `admin` | `1234` | 전체 관리자 |
| `area1` | `1234` | 지역 관리자 1 |
| `area2` | `1234` | 지역 관리자 2 |
| `area3` | `1234` | 지역 관리자 3 |

**최초 로그인 후 반드시 비밀번호를 변경하세요.** 초기 비밀번호 상태인 계정은 계정 관리 화면에 `초기비번` 표시가 붙습니다.

---

## 3. 권한 구분

| 기능 | 전체 관리자 | 지역 관리자 | 점포 |
|---|:---:|:---:|:---:|
| 제품 등록 · 상태 변경 | ○ | ○ | ○ (자기 점포) |
| 진행 현황 조회 | 전체 | 자기 지역 | 자기 점포 |
| 점포별 현황 | 전체 | 자기 지역 | – |
| 사용 이력 · CSV 내려받기 | 전체 | 자기 지역 | 자기 점포 |
| 점포 계정 생성 · 수정 · 비번 초기화 | ○ | ○ (자기 지역만) | – |
| 지역 관리자 계정 생성 | ○ | – | – |
| 제품 기준값(해동시간 · 품질기한) 설정 | ○ | 조회만 | – |
| 지역 추가 · 이름 변경 · 삭제 | ○ | – | – |

지역 관리자는 계정 추가 창에서 구분과 지역이 자기 지역으로 고정되어, 다른 지역 데이터에는 접근할 수 없습니다.

---

## 4. 운영 순서

### 전체 관리자
1. **지역 관리** 탭에서 지역명을 실제 명칭으로 바꿉니다 (기본값 `지역 1/2/3`).
2. **제품 기준값** 탭에서 제품별 해동시간과 품질기한을 확인·수정합니다.
3. **계정 관리** 탭에서 지역 관리자 계정을 만들어 각 지역에 전달합니다.

### 지역 관리자
1. **계정 관리** 탭에서 담당 점포 계정을 만들어 점포에 전달합니다.
2. **점포 현황** 탭에서 점포별 등록 건수와 임박·초과 건수를 확인합니다.

### 점포
1. **제품 등록** — 제품과 제조일자를 고르면 해동사용일 · 점포사용일 · 보관방법이 자동 계산됩니다.
2. **진행 현황** — 남은 시간이 카드로 표시됩니다. 판매가 끝나면 `사용 완료`, 기한이 지나면 `폐기` 를 누릅니다.
3. **체크리스트 인쇄** — 기존 종이 양식과 같은 형태로 출력됩니다 (A4 가로).

---

## 5. 자동 계산 규칙

| 항목 | 계산식 |
|---|---|
| 해동사용일 (판매 가능 시작) | 제조일시 + 해동시간 |
| 점포사용일 (품질기한) | 제조일시 + 품질기한 시간 |
| 품질기한이 `당일`(0시간)인 제품 | 제조일 당일 24시 |

품질기한은 **해동시간을 포함한 판매 가능 총 시간**입니다.

해동 12시간 + 품질기한 당일 제품(파니니류 · 햄토스트 등)은 제조 시각이 늦으면 판매 가능 시간이 거의 남지 않습니다. 이 경우 등록 화면에 경고가 표시됩니다.

- 판매 가능 시간 2시간 미만 → 주황 경고
- 해동이 끝나기 전에 품질기한 도달 → 빨강 경고

보관기준 — 실온 1~35℃, 상온 15~25℃, 냉장 0~10℃, 냉동 -18℃ 이하

---

## 6. 데이터 관리

- 모든 기록은 구글 시트 `Records` 탭에 쌓입니다. 시트에서 직접 열어봐도 되고, 앱의 **사용 이력** 탭에서 기간·점포·제품별로 조회할 수 있습니다.
- **엑셀(CSV) 내려받기** 버튼으로 조회 결과를 그대로 받아 엑셀에서 집계할 수 있습니다.
- 이력 화면에서 총 등록 · 사용 완료 · 폐기 건수와 폐기율, 제품별 등록 순위를 바로 볼 수 있습니다.
- 한 번에 3,000건까지 조회됩니다. 그보다 많으면 기간을 좁혀 주세요.

---

## 7. 자주 겪는 문제

| 증상 | 확인할 것 |
|---|---|
| `서버에 연결할 수 없습니다` | 웹 앱 URL 이 `/exec` 로 끝나는지, 인터넷 연결이 되는지 |
| `서버 응답을 읽을 수 없습니다` | 배포 설정의 **액세스 권한이 모든 사용자**인지 |
| 코드를 고쳤는데 반영이 안 됨 | 배포 관리에서 **새 버전**으로 재배포했는지 |
| 로그인이 자꾸 풀림 | 로그인 유지 시간은 12시간입니다 (`SESSION_HOURS` 로 조정) |
| 시트 탭이 없다고 나옴 | Apps Script 에서 `setup` 함수를 실행했는지 |
