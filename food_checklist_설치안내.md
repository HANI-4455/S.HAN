# FOOD 제품 생산 Check-list — 설치 및 운영 안내

PC · 태블릿 브라우저에서 그대로 쓰는 웹 프로그램입니다. 설치 앱이 아니라 주소만 열면 됩니다.

| 파일 | 역할 |
|---|---|
| `food_checklist.html` | 화면 (점포 · 팀 관리자 · 전체 관리자 공용) |
| `food_checklist_gas.js` | 데이터 저장 서버 (Google Apps Script) |

---

## 1. 서버 만들기 (최초 1회, 전체 관리자만)

1. [구글 시트](https://sheets.new)를 새로 만들고 이름을 `FOOD 체크리스트 DB` 로 지정합니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script** 를 엽니다.
3. 편집기에 있던 내용을 모두 지우고 `food_checklist_gas.js` 전체를 붙여넣은 뒤 저장합니다.
4. 상단 함수 목록에서 **`setup`** 을 선택하고 **실행** 을 누릅니다.
   - 처음 실행하면 권한 승인 창이 뜹니다. `고급 → 안전하지 않음(이동)` 을 눌러 본인 계정으로 승인합니다.
   - 실행이 끝나면 시트에 `Regions` `Users` `Products` `Records` 탭과 기본 제품 16종이 생성됩니다.
5. **배포 → 새 배포 → 유형 선택(톱니바퀴) → 웹 앱** 을 고르고 아래처럼 설정합니다.

   | 항목 | 값 |
   |---|---|
   | 실행 사용자 | **나** |
   | 액세스 권한이 있는 사용자 | **모든 사용자** |

6. 배포 후 표시되는 **웹 앱 URL** (`https://script.google.com/macros/s/.../exec`) 을 복사해 둡니다.

> 코드를 수정한 뒤에는 반드시 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포** 를 해야 반영됩니다. 저장만으로는 반영되지 않습니다.

### 이미 쓰던 시트를 최신 코드로 올릴 때

팀 · 지역 기능이 들어가면서 시트에 열이 늘었습니다. 아래 순서로 하면 **기존 데이터는 그대로 남습니다.**

1. Apps Script 편집기 내용을 지우고 최신 `food_checklist_gas.js` 를 붙여넣고 저장
2. 함수 목록에서 **`setup`** 실행 — 늘어난 열의 머리글이 채워지고, 임시 팀명(`지역 1/2/3`)은 `사업1팀/2팀/3팀` 으로 바뀝니다
3. **배포 → 배포 관리 → 편집 → 버전: 새 버전 → 배포**
4. 앱에서 **팀 · 지역 관리** 탭을 열어 팀마다 담당 지역을 지정
5. **계정 관리** 탭에서 기존 점포 계정을 하나씩 열어 **지역**을 지정 (지정 전에는 `미지정` 으로 표시됩니다)

⚠ 5번을 하지 않으면 지역별 집계에서 `(지역 미지정)` 으로 묶입니다. 이미 쌓인 이력의 지역 값은 소급되지 않고, 지역을 지정한 뒤 등록하는 기록부터 반영됩니다.

---

## 2. 접속하기

주소: **https://hani-4455.github.io/S.HAN/food_checklist.html**

**점포에서는 서버 주소를 입력할 필요가 없습니다.** 주소가 코드에 들어 있어 열면 바로 로그인 화면이 나옵니다.

### 앱으로 설치하기 (권장)

주소창 없이 앱처럼 전체 화면으로 쓰려면 한 번만 아래를 하면 됩니다. 홈 화면에 파란 체크 아이콘이 생깁니다.

| 기기 | 방법 |
|---|---|
| 아이폰 · 아이패드 | **사파리**로 열고 → 하단 **공유** 버튼 → **홈 화면에 추가** |
| 안드로이드 | 크롬으로 열고 → 메뉴(⋮) → **홈 화면에 추가** |
| PC (크롬 · 엣지) | 주소창 오른쪽 **설치** 아이콘 클릭 |

아이폰은 반드시 **사파리**로 해야 합니다. 크롬 등 다른 브라우저에서는 홈 화면 추가가 되지 않습니다.

한 번 설치하면 화면과 글꼴이 기기에 저장되어, 와이파이가 느리거나 잠시 끊겨도 화면은 바로 뜹니다. 다만 등록·저장은 인터넷이 연결되어야 합니다.

### 서버 주소를 바꿔야 할 때 (관리자만)

Apps Script 를 **새 배포**로 다시 올려 URL 이 바뀌면 `food_checklist.html` 안의 `DEFAULT_API` 값을 고쳐 다시 올리면 모든 기기에 반영됩니다.

특정 기기만 다른 서버에 붙이려면 **전체 관리자(`admin`)로 로그인한 뒤** 우측 상단 **서버 주소** 버튼을 누릅니다. 이 기기에만 적용되고, 비우고 저장하면 기본 주소로 돌아갑니다.

점포 계정과 팀 관리자 계정에는 이 버튼이 보이지 않습니다. 잘못 건드려 그 기기에서 접속이 끊기는 것을 막기 위해서입니다.

서버가 멈춰 로그인 자체가 안 될 때는 주소 끝에 `?server=1` 을 붙여 열면 로그인 화면에서도 주소를 고칠 수 있습니다.

```
https://hani-4455.github.io/S.HAN/food_checklist.html?server=1
```

### 기본 계정 (setup 실행 시 자동 생성)

| 아이디 | 비밀번호 | 권한 |
|---|---|---|
| `admin` | `1234` | 전체 관리자 |
| `area1` | `1234` | 사업1팀 관리자 |
| `area2` | `1234` | 사업2팀 관리자 |
| `area3` | `1234` | 사업3팀 관리자 |

**최초 로그인 후 반드시 비밀번호를 변경하세요.** 초기 비밀번호 상태인 계정은 계정 관리 화면에 `초기비번` 표시가 붙습니다.

---

## 3. 권한 구분

| 기능 | 전체 관리자 | 팀 관리자 | 점포 |
|---|:---:|:---:|:---:|
| 제품 등록 · 상태 변경 | ○ | ○ | ○ (자기 점포) |
| 진행 현황 조회 | 전체 | 자기 팀 | 자기 점포 |
| 데이터 관리 (팀별 · 지역별 집계) | 전체 | 자기 팀 | – |
| 점포별 현황 | 전체 | 자기 팀 | – |
| 사용 이력 · CSV 내려받기 | 전체 | 자기 팀 | 자기 점포 |
| 점포 계정 생성 · 수정 · 비번 초기화 | ○ | ○ (자기 팀만) | – |
| 팀 관리자 계정 생성 | ○ | – | – |
| 제품 기준값(해동시간 · 품질기한) 설정 | ○ | 조회만 | – |
| 팀 추가 · 이름 변경 · 담당 지역 지정 | ○ | – | – |

팀 관리자는 계정 추가 창에서 구분과 팀이 자기 팀으로 고정되고, 지역도 담당 지역 안에서만 고를 수 있습니다. 다른 팀 데이터에는 접근할 수 없습니다.

### 팀과 지역

- **팀** — 사업1팀 · 사업2팀 · 사업3팀. 관리 단위이며 팀마다 관리자가 한 명씩 있습니다.
- **지역** — 전국 광역시도 17곳 (서울 · 부산 · 대구 · 인천 · 광주 · 대전 · 울산 · 세종 · 경기 · 강원 · 충북 · 충남 · 전북 · 전남 · 경북 · 경남 · 제주). 점포가 실제로 있는 곳입니다.

팀마다 담당 지역을 지정하면, 그 팀의 점포 계정은 담당 지역 안에서만 만들 수 있습니다. **한 지역을 두 팀이 함께 담당할 수는 없습니다.**

---

## 4. 운영 순서

### 전체 관리자
1. **팀 · 지역 관리** 탭에서 팀 이름을 확인하고(기본값 `사업1팀/2팀/3팀`), 팀마다 **담당 지역**을 체크합니다.
2. **제품 기준값** 탭에서 제품별 해동시간과 품질기한을 확인·수정합니다.
3. **계정 관리** 탭에서 팀 관리자 계정을 만들어 각 팀에 전달합니다.
4. **데이터 관리** 탭에서 팀별 · 지역별 등록 · 폐기 현황을 확인합니다.

### 팀 관리자
1. **계정 관리** 탭에서 담당 점포 계정을 만들어 점포에 전달합니다.
2. **점포 현황** 탭에서 점포별 등록 건수와 임박·초과 건수를 확인합니다.

### 점포
1. **제품 등록** — 기존 종이 양식과 똑같은 표가 그대로 뜹니다.
   - 빈 칸(`＋`)을 누르면 제품과 제조일자를 고르는 창이 열립니다.
   - **해동사용일 · 점포사용일 · 보관방법은 자동으로 채워집니다.**
   - 오른쪽 기준표에서 제품을 눌러 선택한 칸에 바로 넣을 수도 있습니다.
   - 보관방법 칸(실온/상온/냉장/냉동)은 직접 눌러 바꿀 수 있습니다. 판 포카챠처럼 기준이 둘인 제품만 바뀌고, 나머지는 안내가 뜹니다.
   - 여러 칸을 채운 뒤 **저장** 을 한 번 누르면 한꺼번에 등록됩니다. 저장 전에는 `미저장` 표시가 붙습니다.
   - 칸이 모자라면 **＋ 칸 추가**, 종이로 쓰려면 **인쇄** 를 누릅니다 (A4 가로).
   - 이미 저장된 칸을 누르면 `사용 완료` · `폐기` 처리와 삭제를 할 수 있습니다.
2. **진행 현황** — 남은 시간이 카드로 표시됩니다. 기한 임박·초과 건을 한눈에 보기 좋습니다.

제조일자를 잘못 넣어 품질기한이 이미 지났거나, 해동이 끝나기 전에 기한이 닿는 경우에는 확인 창이 뜹니다. 실제로 그런 상황이면 그대로 진행하고, 아니면 제조일자를 다시 확인하세요.

---

## 5. 자동 계산 규칙

| 항목 | 계산식 |
|---|---|
| 해동사용일 (판매 가능 시작) | 제조일시 + 해동시간 |
| 점포사용일 (품질기한) | 제조일시 + 품질기한 시간 |
| 품질기한이 `당일`(0시간)인 제품 | 제조일 당일 24시 |

품질기한은 **해동시간을 포함한 판매 가능 총 시간**입니다.

해동 12시간 + 품질기한 당일 제품(파니니류 · 햄토스트 등)은 제조 시각이 늦으면 판매 가능 시간이 거의 남지 않습니다. 이 경우 등록 화면에 경고가 표시됩니다.

- 판매 가능 시간 2시간 미만 → 주황 경고
- 해동이 끝나기 전에 품질기한 도달 → 빨강 경고

보관기준 — 실온 1~35℃, 상온 15~25℃, 냉장 0~10℃, 냉동 -18℃ 이하

---

## 6. 데이터 관리

**데이터 관리** 탭 (전체 관리자 · 팀 관리자)

- 조회 기간을 오늘 / 7일 / 30일 / 90일 로 바꿔가며 봅니다.
- 상단 요약 — 총 등록, 사용 완료, 폐기와 폐기율, 현재 진행중, 기한 임박, 기한 초과
- **팀별 현황** — 팀마다 담당 지역, 점포 수, 등록 · 완료 · 폐기 · 폐기율 · 진행중 · 임박 · 초과
- **지역별 현황** — 광역시도별로 같은 지표. 담당 팀이 함께 표시됩니다.
- **집계 내려받기** — 팀 · 지역 · 점포 세 단위 집계를 한 CSV 로 받습니다.

**사용 이력** 탭

- 기간 · 점포 · 제품별 조회, 제품별 등록 순위와 폐기율
- **엑셀(CSV) 내려받기** — 팀 · 지역 열이 포함되어 엑셀 피벗으로 바로 집계할 수 있습니다.

그 밖에

- 모든 기록은 구글 시트 `Records` 탭에 쌓입니다. 시트에서 직접 열어봐도 됩니다.
- 한 번에 3,000건까지 조회됩니다. 그보다 많으면 기간을 좁혀 주세요.

---

## 7. 자주 겪는 문제

| 증상 | 확인할 것 |
|---|---|
| `서버에 연결할 수 없습니다` | 웹 앱 URL 이 `/exec` 로 끝나는지, 인터넷 연결이 되는지 |
| `서버 응답을 읽을 수 없습니다` | 배포 설정의 **액세스 권한이 모든 사용자**인지 |
| 코드를 고쳤는데 반영이 안 됨 | 배포 관리에서 **새 버전**으로 재배포했는지 |
| 로그인이 자꾸 풀림 | 로그인 유지 시간은 12시간입니다 (`SESSION_HOURS` 로 조정) |
| 시트 탭이 없다고 나옴 | Apps Script 에서 `setup` 함수를 실행했는지 |
| 화면을 고쳤는데 앱에서 옛날 화면이 나옴 | 주소 끝에 `?nosw=1` 을 붙여 한 번 연 뒤 새로고침하면 저장된 캐시가 지워집니다 |
| 아이폰에서 홈 화면에 추가가 안 보임 | 크롬이 아니라 **사파리**로 열었는지 |
